# YOLO 검출 학습용 격리 환경 패키지
# 설치: pip install -r requirements.txt  (venv 활성화 상태에서)
# 주의: torch 기본 PyPI 휠은 CUDA 런타임을 내장하므로 대개 그대로 GPU 인식됨.
#       머신 CUDA와 안 맞으면 아래처럼 특정 빌드 지정:
#         pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.2
torchvision>=0.17
ultralytics>=8.3.0
opencv-python-headless>=4.9   # headless: 서버에서 libGL 없이 동작
matplotlib>=3.7
pandas>=2.0
numpy>=1.24
